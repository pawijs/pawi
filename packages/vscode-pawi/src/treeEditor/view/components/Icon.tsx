// import { Dialog } from '@forten/dialog'
import { Icon as SIcon } from '@forten/styled'
import { styled } from '../app'

export const Icon = styled(SIcon)`
  color: #555;
  &.highlighted {
    color: #999;
  }
`
