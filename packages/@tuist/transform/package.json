{
  "name": "@tuist/transform",
  "version": "0.0.0",
  "description": "Code transform for live scrubbing.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tuist-org/tuist.git"
  },
  "author": "Maia Taewana <maia@tuitui.st>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "tsc -p setup/tsconfig.${NODE_ENV:=development}.json",
    "clean": "rimraf .cache dist coverage ../../node_modules/@tuist/transform",
    "postbuild": "cp package.json ../../node_modules/@tuist/transform",
    "prepare": "rimraf dist && cp -r ../../node_modules/@tuist/transform/dist dist",
    "stories": "parcel serve src/stories/index.html",
    "test:watch": "jest -c setup/jest.js --watch --coverage false",
    "test": "jest -c setup/jest.js"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^4.1.5"
  }
}