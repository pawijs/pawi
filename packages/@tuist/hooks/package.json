{
  "name": "@tuist/hooks",
  "version": "0.0.0",
  "description": "Support for callable hooks in actions (through effects).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tuist-org/tuist.git"
  },
  "author": "Maia Taewana <maia@tuitui.st>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "tsc -p setup/tsconfig.${NODE_ENV:=development}.json",
    "postbuild": "cp package.json ../../node_modules/@tuist/hooks",
    "prepare": "rimraf dist && cp -r ../../node_modules/@tuist/hooks/dist dist",
    "clean": "rimraf .cache dist coverage ../../node_modules/@tuist/hooks",
    "test": "jest -c setup/jest.js",
    "test:watch": "jest -c setup/jest.js --watch --coverage false"
  },
  "dependencies": {
    "@tuist/build": "next"
  },
  "devDependencies": {}
}